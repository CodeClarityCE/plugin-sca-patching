services:
  plugin-js-patching:
    # For dev we keep the golang container and install devtools
    build:
      target: plugin-debug
    image: ceherzog/js-patching-debug:latest
    ports:
      - 40000:40000
